# MongoDB Atlas connection string (no quotes). Example:
# mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Limousine
MONGODB_URI=

# Optional: database name (defaults to limousine_db if not set)
DB_NAME=limousine_db
